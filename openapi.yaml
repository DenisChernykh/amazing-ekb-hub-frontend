# yaml-language-server: $schema=https://spec.openapis.org/oas/3.0/schema/2021-09-28
openapi: 3.0.3
info:
  title: Amazing EKB Hub API
  version: 1.0.0
  description: MVP backend API (OpenAPI-first)
servers:
  - url: /v1
tags:
  - name: Auth
    description: Аутентификация пользователя, refresh токенов и получение текущего профиля.
  - name: Places
    description: Публичные эндпоинты каталога мест и связанных с ними материалов.
  - name: Favorites
    description: Работа с избранными местами текущего пользователя.
  - name: Admin
    description: Административные операции управления местами и материалами.
  - name: Health
    description: Служебные эндпоинты liveness/readiness для мониторинга и orchestration.

components:
  securitySchemes:
    AccessCookieAuth:
      type: apiKey
      in: cookie
      name: aeh_access_token
      description: |
        HttpOnly cookie с JWT access token. Клиент должен отправлять запросы с credentials.
    RefreshCookieAuth:
      type: apiKey
      in: cookie
      name: aeh_refresh_token
      description: |
        HttpOnly cookie с refresh token для `/auth/refresh` и `/auth/logout`.

  parameters:
    PlaceId:
      name: placeId
      in: path
      required: true
      description: Идентификатор места.
      schema:
        type: string
      example: place_ekb_001

    PlaceSlug:
      name: placeSlug
      in: path
      required: true
      description: Публичный slug места.
      schema:
        type: string
        pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
      example: baden-baden-uktus

    CategoryId:
      name: categoryId
      in: path
      required: true
      description: Идентификатор категории места.
      schema:
        type: string
      example: category_spa

    CategorySlug:
      name: categorySlug
      in: path
      required: true
      description: Публичный slug категории места.
      schema:
        type: string
        pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
      example: spa

    MaterialId:
      name: materialId
      in: path
      required: true
      description: Идентификатор материала.
      schema:
        type: string
      example: material_telegram_001

    ContentSourceId:
      name: sourceId
      in: path
      required: true
      description: Идентификатор content source.
      schema:
        type: string
      example: source_telegram_001

    PlaceImportOperationId:
      name: operationId
      in: path
      required: true
      description: Идентификатор операции автоматического создания места.
      schema:
        type: string
      example: cmrvo_place_import_001

    Page:
      name: page
      in: query
      required: false
      description: Номер страницы пагинации. Допустимый диапазон от `1` до `1000`.
      schema:
        type: integer
        minimum: 1
        maximum: 1000
        default: 1
      example: 1

    PageSize:
      name: pageSize
      in: query
      required: false
      description: Размер страницы. Допустимый диапазон от `1` до `100`.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
      example: 20

  examples:
    AuthLoginRequest:
      summary: Вход по email и паролю
      value:
        email: admin@example.test
        password: replace_me_password

    AuthMeResponse:
      summary: Профиль текущего пользователя
      value:
        id: user_admin_001
        email: admin@example.test
        role: admin

    PlaceSummary:
      summary: Краткая карточка места
      value:
        id: place_ekb_001
        title: Bаден-Баден Уктус
        summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          - термы
          - spa
          - бассейн
        category:
          id: category_spa
          slug: spa
          title: SPA
          coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
        status: active
        slug: baden-baden-uktus
        coverImageUrl: /v1/places/baden-baden-uktus/photo

    PublicPlaceSummary:
      summary: Краткая публичная карточка места
      value:
        id: place_ekb_001
        title: Bаден-Баден Уктус
        summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          - термы
          - spa
          - бассейн
        category:
          id: category_spa
          slug: spa
          title: SPA
          coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
        status: active
        slug: baden-baden-uktus
        coverImageUrl: /v1/places/baden-baden-uktus/photo
        counters:
          dzen: 12
          telegram: 7
          instagram: 3

    PlaceDetail:
      summary: Детальная карточка места
      value:
        id: place_ekb_001
        title: Bаден-Баден Уктус
        summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          - термы
          - spa
          - бассейн
        category:
          id: category_spa
          slug: spa
          title: SPA
          coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
        status: active
        slug: baden-baden-uktus
        coverImageUrl: /v1/places/baden-baden-uktus/photo
        mapsUrl: https://yandex.ru/maps/org/baden_baden/123456789
        pinnedMaterial:
          id: material_telegram_001
          placeId: place_ekb_001
          platform: telegram
          type: post
          title: null
          publishedAt: '2026-03-20'
          durationSec: null
          redirectUrl: /v1/materials/material_telegram_001/go
        counters:
          dzen: 12
          telegram: 7
          instagram: 3

    PlaceListResponse:
      summary: Пагинированный список мест
      value:
        items:
          - id: place_ekb_001
            title: Bаден-Баден Уктус
            summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
            tags:
              - термы
              - spa
            category:
              id: category_spa
              slug: spa
              title: SPA
              coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
            status: active
            slug: baden-baden-uktus
            coverImageUrl: /v1/places/baden-baden-uktus/photo
          - id: place_ekb_002
            title: The Pool
            summary: Современный бассейн с дорожками и семейной зоной.
            tags:
              - бассейн
              - family
            category:
              id: category_pools
              slug: pools
              title: Бассейны
              coverImageUrl: /v1/categories/pools/photo?v=4e8a65a4732f
            status: active
            slug: the-pool
            coverImageUrl: null
        total: 2
        page: 1
        pageSize: 20

    AdminPlaceListResponse:
      summary: Административный пагинированный список мест
      value:
        items:
          - id: place_ekb_001
            title: Bаден-Баден Уктус
            summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
            tags:
              - термы
              - spa
            category:
              id: category_spa
              slug: spa
              title: SPA
              coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
            status: hidden
            slug: baden-baden-uktus
            coverImageUrl: /v1/places/baden-baden-uktus/photo
            mapsUrl: https://yandex.ru/maps/org/baden_baden/123456789
        total: 1
        page: 1
        pageSize: 20

    PublicPlaceListResponse:
      summary: Публичный пагинированный список мест
      value:
        items:
          - id: place_ekb_001
            title: Bаден-Баден Уктус
            summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
            tags:
              - термы
              - spa
            category:
              id: category_spa
              slug: spa
              title: SPA
              coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
            status: active
            slug: baden-baden-uktus
            coverImageUrl: /v1/places/baden-baden-uktus/photo
            counters:
              dzen: 12
              telegram: 7
              instagram: 3
          - id: place_ekb_002
            title: The Pool
            summary: Современный бассейн с дорожками и семейной зоной.
            tags:
              - бассейн
              - family
            category:
              id: category_pools
              slug: pools
              title: Бассейны
              coverImageUrl: /v1/categories/pools/photo?v=4e8a65a4732f
            status: active
            slug: the-pool
            coverImageUrl: null
            counters:
              dzen: 0
              telegram: 2
              instagram: 1
        total: 2
        page: 1
        pageSize: 20

    PlaceCategory:
      summary: Категория места
      value:
        id: category_spa
        slug: spa
        title: SPA
        coverImageUrl: /v1/categories/spa/photo?v=23b991827df4

    AdminPlaceCategory:
      summary: Административная категория места
      value:
        id: category_spa
        slug: spa
        title: SPA
        status: active
        coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
        createdAt: '2026-07-03T10:00:00.000Z'
        updatedAt: '2026-07-03T10:00:00.000Z'

    PlaceCategoryListResponse:
      summary: Публичный список категорий мест
      value:
        items:
          - id: category_spa
            slug: spa
            title: SPA
            coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
          - id: category_pools
            slug: pools
            title: Бассейны
            coverImageUrl: null

    AdminPlaceCategoryListResponse:
      summary: Административный список категорий мест
      value:
        items:
          - id: category_spa
            slug: spa
            title: SPA
            status: active
            coverImageUrl: /v1/categories/spa/photo?v=23b991827df4
            createdAt: '2026-07-03T10:00:00.000Z'
            updatedAt: '2026-07-03T10:00:00.000Z'

    Material:
      summary: Материал о месте
      value:
        id: material_telegram_001
        placeId: place_ekb_001
        platform: telegram
        type: post
        title: null
        publishedAt: '2026-03-20'
        durationSec: null
        url: https://t.me/amazing_ekb/321

    MaterialListResponse:
      summary: Список материалов места
      value:
        items:
          - id: material_telegram_001
            placeId: place_ekb_001
            platform: telegram
            type: post
            title: null
            publishedAt: '2026-03-20'
            durationSec: null
            redirectUrl: /v1/materials/material_telegram_001/go
          - id: material_dzen_002
            placeId: place_ekb_001
            platform: dzen
            type: video
            title: Видеообзор термального комплекса
            publishedAt: '2026-03-22'
            durationSec: 183
            redirectUrl: /v1/materials/material_dzen_002/go

    AdminMaterialLibraryListResponse:
      summary: Список материалов общей библиотеки
      value:
        items:
          - id: material_telegram_001
            platform: telegram
            type: post
            title: null
            publishedAt: '2026-03-20'
            durationSec: null
            url: https://t.me/amazing_ekb/321
            source:
              id: source_telegram_001
              platform: telegram
              displayName: Amazing EKB Telegram
              url: https://t.me/amazing_ekb
            externalId: '321'
            text: 'Пост из Telegram-канала Amazing EKB'
            excerpt: 'Пост из Telegram-канала Amazing EKB'
            mediaKind: photo
            mediaPreviewUrl: null
            adminStatus: approved
            linked: true
            placeLink: active
          - id: material_dzen_002
            platform: dzen
            type: video
            title: Видеообзор термального комплекса
            publishedAt: '2026-03-22'
            durationSec: 183
            url: https://dzen.ru/video/watch/abcdef
            source: null
            externalId: null
            text: null
            excerpt: null
            mediaKind: null
            mediaPreviewUrl: null
            adminStatus: approved
            linked: false
            placeLink: null
        total: 2
        page: 1
        pageSize: 100

    ContentSource:
      summary: Пользовательский content source
      value:
        id: source_telegram_001
        platform: telegram
        displayName: Amazing EKB Telegram
        url: https://t.me/amazing_ekb
        externalId: '123456'
        handle: amazing_ekb
        channelId: null
        status: active
        lastImportedAt: null
        lastCursor: null
        createdAt: '2026-05-20T10:00:00.000Z'
        updatedAt: '2026-05-20T10:00:00.000Z'

    ContentSourceListResponse:
      summary: Список пользовательских content sources
      value:
        items:
          - id: source_telegram_001
            platform: telegram
            displayName: Amazing EKB Telegram
            url: https://t.me/amazing_ekb
            externalId: '123456'
            handle: amazing_ekb
            channelId: null
            status: active
            lastImportedAt: null
            lastCursor: null
            createdAt: '2026-05-20T10:00:00.000Z'
            updatedAt: '2026-05-20T10:00:00.000Z'

    ImportRun:
      summary: Диагностическая запись попытки импорта
      value:
        id: import_run_001
        sourceId: source_telegram_001
        status: completed
        startedAt: '2026-05-26T05:00:00.000Z'
        finishedAt: '2026-05-26T05:01:00.000Z'
        foundCount: 7
        createdCount: 2
        updatedCount: 4
        skippedDuplicateCount: 1
        errorMessage: null
        createdAt: '2026-05-26T05:00:00.000Z'
        updatedAt: '2026-05-26T05:01:00.000Z'

    QueuedImportRun:
      summary: Queued import run после HTTP trigger-а
      value:
        id: import_run_queued_001
        sourceId: source_telegram_001
        status: queued
        startedAt: null
        finishedAt: null
        foundCount: 0
        createdCount: 0
        updatedCount: 0
        skippedDuplicateCount: 0
        errorMessage: null
        createdAt: '2026-05-26T05:00:00.000Z'
        updatedAt: '2026-05-26T05:00:00.000Z'

    ImportRunListResponse:
      summary: Список попыток импорта
      value:
        items:
          - id: import_run_001
            sourceId: source_telegram_001
            status: completed
            startedAt: '2026-05-26T05:00:00.000Z'
            finishedAt: '2026-05-26T05:01:00.000Z'
            foundCount: 7
            createdCount: 2
            updatedCount: 4
            skippedDuplicateCount: 1
            errorMessage: null
            createdAt: '2026-05-26T05:00:00.000Z'
            updatedAt: '2026-05-26T05:01:00.000Z'

    ValidationError:
      summary: Ошибка валидации входных данных
      value:
        statusCode: 400
        message:
          - page must not be less than 1
        error: Bad Request

    UnauthorizedError:
      summary: Пользователь не аутентифицирован
      value:
        statusCode: 401
        message: Authentication required
        error: Unauthorized

    ForbiddenError:
      summary: Недостаточно прав
      value:
        statusCode: 403
        message: Insufficient role
        error: Forbidden

    TooManyRequestsError:
      summary: Слишком много auth-запросов
      value:
        statusCode: 429
        message: Too many auth attempts, please retry later

    PlaceNotFoundError:
      summary: Место не найдено
      value:
        statusCode: 404
        message: Place not found
        error: Not Found

    CategoryNotFoundError:
      summary: Категория не найдена
      value:
        statusCode: 404
        message: Category not found
        error: Not Found

    CategoryConflictError:
      summary: Категория уже существует
      value:
        statusCode: 409
        message: Category with this slug already exists
        error: Conflict

    CategoryInUseError:
      summary: Категория используется местами
      value:
        statusCode: 409
        message: Category is used by places
        error: Conflict

    MaterialNotFoundError:
      summary: Материал не найден
      value:
        statusCode: 404
        message: Material not found
        error: Not Found

    ContentSourceNotFoundError:
      summary: Content source не найден
      value:
        statusCode: 404
        message: Content source not found
        error: Not Found

    ContentSourceConflictError:
      summary: Content source уже существует
      value:
        statusCode: 409
        message: Content source already exists for this platform and URL
        error: Conflict

    ContentSourceIdentityLockedError:
      summary: Identity content source уже использовался для импортов
      value:
        statusCode: 409
        message: Content source identity cannot be changed after imports have started
        error: Conflict

    TelegramImportAlreadyRunningError:
      summary: Telegram import уже выполняется
      value:
        statusCode: 409
        message: Telegram import is already running for this content source
        error: Conflict

    PinnedMaterialBadRequestError:
      summary: Материал не принадлежит месту
      value:
        statusCode: 400
        message: Pinned material must belong to the same place
        error: Bad Request

    ServiceUnavailableError:
      summary: Сервис временно не готов
      value:
        statusCode: 503
        message: Database is not ready
        error: Service Unavailable

    HealthLiveResponse:
      summary: Приложение живо
      value:
        status: ok

    HealthReadyResponse:
      summary: Приложение готово обслуживать запросы
      value:
        status: ok
        checks:
          database: up

  responses:
    ValidationError:
      description: Ошибка валидации входных параметров или тела запроса.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/ValidationError'

    Unauthorized:
      description: Пользователь не аутентифицирован или auth cookie отсутствует/некорректна.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/UnauthorizedError'

    Forbidden:
      description: У текущего пользователя недостаточно прав для выполнения операции.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/ForbiddenError'

    TooManyRequests:
      description: Слишком много auth-запросов за короткий промежуток времени.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/TooManyRequestsError'

    PlaceNotFound:
      description: Указанное место не найдено.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/PlaceNotFoundError'

    CategoryNotFound:
      description: Указанная категория не найдена.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/CategoryNotFoundError'

    CategoryConflict:
      description: Категория конфликтует с существующей записью или используется местами.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            duplicate:
              $ref: '#/components/examples/CategoryConflictError'
            inUse:
              $ref: '#/components/examples/CategoryInUseError'

    MaterialNotFound:
      description: Указанный материал не найден.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/MaterialNotFoundError'

    ContentSourceNotFound:
      description: Указанный content source не найден.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/ContentSourceNotFoundError'

    ContentSourceConflict:
      description: Content source с такой platform/url уже существует или identity source уже нельзя менять после старта импортов.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            duplicate:
              $ref: '#/components/examples/ContentSourceConflictError'
            identityLocked:
              $ref: '#/components/examples/ContentSourceIdentityLockedError'

    TelegramImportAlreadyRunning:
      description: Для этого content source уже есть active queued/running Telegram import run.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/TelegramImportAlreadyRunningError'

    ServiceUnavailable:
      description: Сервис временно не готов обрабатывать запросы.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/NestErrorResponse'
          examples:
            default:
              $ref: '#/components/examples/ServiceUnavailableError'

  schemas:
    Role:
      type: string
      description: Роль пользователя в системе.
      enum: [admin, user]
      example: admin

    PlaceCategory:
      type: object
      description: Публичная категория места для фильтров.
      required: [id, slug, title, coverImageUrl]
      properties:
        id:
          type: string
          description: Идентификатор категории.
          example: category_spa
        slug:
          type: string
          description: Человекочитаемый slug категории.
          example: spa
        title:
          type: string
          description: Название категории для интерфейса.
          example: SPA
        coverImageUrl:
          type: string
          nullable: true
          description: Versioned URL cover-фотографии категории или `null`, если фото отсутствует.
          example: /v1/categories/spa/photo?v=23b991827df4

    AdminPlaceCategory:
      allOf:
        - $ref: '#/components/schemas/PlaceCategory'
        - type: object
          description: Административная категория места с системными датами.
          required: [status, createdAt, updatedAt]
          properties:
            status:
              type: string
              enum: [draft, active]
              description: Draft-категория скрыта из public API до публикации первого места.
              example: active
            createdAt:
              type: string
              format: date-time
              description: Время создания категории.
              example: '2026-07-03T10:00:00.000Z'
            updatedAt:
              type: string
              format: date-time
              description: Время последнего обновления категории.
              example: '2026-07-03T10:00:00.000Z'

    PlaceCategoryListResponse:
      type: object
      description: Публичный список категорий мест.
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PlaceCategory'

    AdminPlaceCategoryListResponse:
      type: object
      description: Административный список категорий мест.
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminPlaceCategory'

    PlaceStatus:
      type: string
      description: Статус публикации места.
      enum: [active, hidden]
      example: active

    Platform:
      type: string
      description: Платформа, на которой опубликован материал.
      enum: [dzen, telegram, instagram]
      example: telegram

    MaterialType:
      type: string
      description: Тип материала.
      enum: [post, reel, video]
      example: post

    MaterialAdminStatus:
      type: string
      description: Review-статус материала в административной библиотеке.
      enum: [pending, approved, rejected, archived]
      example: approved

    PlaceMaterialLinkStatus:
      type: string
      description: Статус связи библиотечного материала с конкретным местом.
      enum: [active, hidden]
      example: active

    ContentSourcePlatform:
      type: string
      description: Платформа пользовательского источника контента. Этот enum отделен от material `Platform`, чтобы будущие источники не меняли публичные счетчики материалов.
      enum: [telegram, dzen, instagram, tiktok, vk, pinterest]
      example: telegram

    ContentSourceStatus:
      type: string
      description: Статус пользовательского content source.
      enum: [active, disabled]
      example: active

    ImportRunStatus:
      type: string
      description: Статус попытки импорта материалов.
      enum: [queued, running, completed, failed]
      example: completed

    NestErrorResponse:
      type: object
      description: Стандартный JSON body, который NestJS возвращает для `HttpException`.
      required: [statusCode, message]
      properties:
        statusCode:
          type: integer
          description: HTTP status code ответа.
          example: 400
        message:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
          description: Сообщение ошибки. Для DTO validation NestJS обычно возвращает массив строк.
          example: Bad Request
        error:
          type: string
          description: Стандартное HTTP reason summary от NestJS.
          example: Bad Request

    AuthLoginRequest:
      type: object
      description: Данные для входа пользователя по email и паролю.
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          description: Email пользователя.
          example: admin@example.test
        password:
          type: string
          description: Пароль пользователя. Login не раскрывает password policy и возвращает generic credentials error для неверного пароля любой длины.
          example: replace_me_password

    AuthMeResponse:
      type: object
      description: Публичный профиль текущего пользователя.
      required: [id, email, role]
      properties:
        id:
          type: string
          description: Идентификатор пользователя.
          example: user_admin_001
        email:
          type: string
          format: email
          description: Email пользователя.
          example: admin@example.test
        role:
          $ref: '#/components/schemas/Role'

    PlaceSummary:
      type: object
      description: Краткая карточка места, используемая в списках.
      required: [id, slug, title, summary, tags, category, status, coverImageUrl]
      properties:
        id:
          type: string
          description: Идентификатор места.
          example: place_ekb_001
        slug:
          type: string
          description: Публичный slug места.
          example: baden-baden-uktus
        title:
          type: string
          description: Название места.
          example: Bаден-Баден Уктус
        summary:
          type: string
          description: Короткое описание для каталога.
          example: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          type: array
          description: Набор тегов для поиска и фильтрации.
          items:
            type: string
          example:
            - термы
            - spa
            - бассейн
        category:
          $ref: '#/components/schemas/PlaceCategory'
        status:
          $ref: '#/components/schemas/PlaceStatus'
        coverImageUrl:
          type: string
          nullable: true
          description: Публичный cover-фото места. Если фото отсутствует или не должно отдаться публично, возвращается `null`.
          example: /v1/places/baden-baden-uktus/photo

    AdminPlaceSummary:
      allOf:
        - $ref: '#/components/schemas/PlaceSummary'
        - type: object
          required: [mapsUrl]
          properties:
            mapsUrl:
              type: string
              format: uri
              nullable: true
              description: Canonical Yandex Maps URL импортированного места; `null` для мест без такой external reference.
              example: https://yandex.ru/maps/org/baden_baden/123456789

    PublicPlaceSummary:
      type: object
      description: Краткая публичная карточка места со счетчиками материалов по платформам.
      required: [id, slug, title, summary, tags, category, status, coverImageUrl, counters]
      properties:
        id:
          type: string
          description: Идентификатор места.
          example: place_ekb_001
        slug:
          type: string
          description: Публичный slug места.
          example: baden-baden-uktus
        title:
          type: string
          description: Название места.
          example: Bаден-Баден Уктус
        summary:
          type: string
          description: Короткое описание для каталога.
          example: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          type: array
          description: Набор тегов для поиска и фильтрации.
          items:
            type: string
          example:
            - термы
            - spa
            - бассейн
        category:
          $ref: '#/components/schemas/PlaceCategory'
        status:
          $ref: '#/components/schemas/PlaceStatus'
        coverImageUrl:
          type: string
          nullable: true
          description: Публичный cover-фото места. Если фото отсутствует или не должно отдаться публично, возвращается `null`.
          example: /v1/places/baden-baden-uktus/photo
        counters:
          type: object
          description: Количество материалов по платформам.
          required: [dzen, telegram, instagram]
          properties:
            dzen:
              type: integer
              example: 12
            telegram:
              type: integer
              example: 7
            instagram:
              type: integer
              example: 3

    Material:
      type: object
      description: Административный материал, связанный с местом. Содержит исходную внешнюю ссылку для внутренних сценариев управления.
      required: [id, placeId, platform, type, title, publishedAt, url, durationSec]
      properties:
        id:
          type: string
          description: Идентификатор материала.
          example: material_telegram_001
        placeId:
          type: string
          description: Идентификатор места, к которому относится материал.
          example: place_ekb_001
        platform:
          $ref: '#/components/schemas/Platform'
        type:
          $ref: '#/components/schemas/MaterialType'
        title:
          type: string
          nullable: true
          description: Заголовок материала. Для импортированных материалов может быть `null`, если источник не дает надежный ручной title.
          example: null
        publishedAt:
          type: string
          format: date
          description: Календарная дата публикации материала в формате `YYYY-MM-DD`.
          example: '2026-03-20'
        durationSec:
          type: integer
          nullable: true
          description: Длительность в секундах для видеоформатов.
          example: 183
        url:
          type: string
          format: uri
          description: Исходная внешняя ссылка на материал. Допускаются только абсолютные http/https URL.
          example: https://t.me/amazing_ekb/321

    PublicMaterial:
      type: object
      description: Публичный материал, связанный с местом. Исходный внешний URL не отдается; публичные клиенты должны использовать `redirectUrl`.
      required: [id, placeId, platform, type, title, publishedAt, durationSec, redirectUrl]
      properties:
        id:
          type: string
          description: Идентификатор материала.
          example: material_telegram_001
        placeId:
          type: string
          description: Идентификатор места, к которому относится материал.
          example: place_ekb_001
        platform:
          $ref: '#/components/schemas/Platform'
        type:
          $ref: '#/components/schemas/MaterialType'
        title:
          type: string
          nullable: true
          description: Заголовок материала. Для импортированных материалов может быть `null`, если источник не дает надежный ручной title.
          example: null
        publishedAt:
          type: string
          format: date
          description: Календарная дата публикации материала в формате `YYYY-MM-DD`.
          example: '2026-03-20'
        durationSec:
          type: integer
          nullable: true
          description: Длительность в секундах для видеоформатов.
          example: 183
        redirectUrl:
          type: string
          nullable: true
          description: Same-origin redirect URL для публичного открытия материала без прямого внешнего href. Поле заполняется только для публично безопасных target URL.
          example: /v1/materials/material_telegram_001/go

    MaterialContentSourceSummary:
      type: object
      description: Краткое представление content source для imported material.
      required: [id, platform, displayName, url]
      properties:
        id:
          type: string
          description: Идентификатор content source.
          example: source_telegram_001
        platform:
          $ref: '#/components/schemas/ContentSourcePlatform'
        displayName:
          type: string
          description: Административное имя source.
          example: Amazing EKB Telegram
        url:
          type: string
          format: uri
          description: URL source.
          example: https://t.me/amazing_ekb

    AdminMaterialLibraryItem:
      type: object
      description: Материал из общей библиотеки для административного интерфейса.
      required:
        [
          id,
          platform,
          type,
          title,
          publishedAt,
          durationSec,
          url,
          source,
          externalId,
          text,
          excerpt,
          mediaKind,
          mediaPreviewUrl,
          adminStatus,
          linked,
          placeLink,
        ]
      properties:
        id:
          type: string
          description: Идентификатор материала.
          example: material_telegram_001
        platform:
          $ref: '#/components/schemas/Platform'
        type:
          $ref: '#/components/schemas/MaterialType'
        title:
          type: string
          nullable: true
          description: Заголовок материала. Для импортированных материалов может быть `null`.
          example: null
        publishedAt:
          type: string
          format: date
          description: Календарная дата публикации материала в формате `YYYY-MM-DD`.
          example: '2026-03-20'
        durationSec:
          type: integer
          nullable: true
          description: Длительность в секундах для видеоформатов.
          example: 183
        url:
          type: string
          format: uri
          description: Публичная ссылка на материал.
          example: https://t.me/amazing_ekb/321
        source:
          nullable: true
          description: Content source, из которого импортирован материал. Для manual materials возвращается `null`.
          oneOf:
            - $ref: '#/components/schemas/MaterialContentSourceSummary'
        externalId:
          type: string
          nullable: true
          description: Платформенный id материала внутри source, например Telegram message id.
          example: '321'
        text:
          type: string
          nullable: true
          description: Полный текст импортированного материала, если он есть.
          example: 'Пост из Telegram-канала Amazing EKB'
        excerpt:
          type: string
          nullable: true
          description: Короткий текстовый preview для админского списка.
          example: 'Пост из Telegram-канала Amazing EKB'
        mediaKind:
          type: string
          nullable: true
          description: Нормализованный тип media из импортера без скачивания бинарных файлов.
          example: photo
        mediaPreviewUrl:
          type: string
          format: uri
          nullable: true
          description: URL preview media, если адаптер смог безопасно его получить.
          example: null
        adminStatus:
          $ref: '#/components/schemas/MaterialAdminStatus'
        linked:
          type: boolean
          description: Есть ли у материала хотя бы одна связь `PlaceMaterial` с любым местом, включая hidden-связи.
          example: false
        placeLink:
          nullable: true
          description: Статус связи с `placeId` из query. Если `placeId` не передан или связи нет, возвращается `null`.
          oneOf:
            - $ref: '#/components/schemas/PlaceMaterialLinkStatus'

    AdminMaterialLibraryListResponse:
      type: object
      description: Пагинированный административный список материалов общей библиотеки.
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          description: Материалы библиотеки на текущей странице.
          items:
            $ref: '#/components/schemas/AdminMaterialLibraryItem'
        total:
          type: integer
          description: Общее количество материалов, подходящих под фильтры.
          example: 42
        page:
          type: integer
          description: Текущая страница.
          example: 1
        pageSize:
          type: integer
          description: Размер страницы.
          example: 100

    ContentSource:
      type: object
      description: 'Пользовательский управляемый источник контента: Telegram-канал, Dzen-канал/профиль или будущий platform resource.'
      required:
        [
          id,
          platform,
          displayName,
          url,
          externalId,
          handle,
          channelId,
          status,
          lastImportedAt,
          lastCursor,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
          description: Идентификатор content source.
          example: source_telegram_001
        platform:
          $ref: '#/components/schemas/ContentSourcePlatform'
        displayName:
          type: string
          description: Административное имя источника, отдельное от заголовков материалов.
          example: Amazing EKB Telegram
        url:
          type: string
          format: uri
          description: Публичный URL управляемого источника. Допускаются только абсолютные http/https URL.
          example: https://t.me/amazing_ekb
        externalId:
          type: string
          nullable: true
          description: Платформенный идентификатор источника, если известен.
          example: '123456'
        handle:
          type: string
          nullable: true
          description: Человекочитаемый handle источника, если он есть.
          example: amazing_ekb
        channelId:
          type: string
          nullable: true
          description: Дополнительный channel id для платформ, где он отличается от externalId/handle.
          example: null
        status:
          $ref: '#/components/schemas/ContentSourceStatus'
        lastImportedAt:
          type: string
          format: date-time
          nullable: true
          description: Время последнего успешного import batch.
          example: null
        lastCursor:
          type: string
          nullable: true
          description: JSON cursor последнего Telegram import batch или cursor будущего платформенного импортера.
          example: null
        createdAt:
          type: string
          format: date-time
          description: Время создания записи.
          example: '2026-05-20T10:00:00.000Z'
        updatedAt:
          type: string
          format: date-time
          description: Время последнего обновления записи.
          example: '2026-05-20T10:00:00.000Z'

    ContentSourceListResponse:
      type: object
      description: Ограниченный административный список пользовательских content sources.
      required: [items]
      properties:
        items:
          type: array
          description: Content sources в стабильном порядке.
          items:
            $ref: '#/components/schemas/ContentSource'

    ImportRun:
      type: object
      description: Диагностическая запись одной попытки импорта материалов.
      required:
        [
          id,
          sourceId,
          status,
          startedAt,
          finishedAt,
          foundCount,
          createdCount,
          updatedCount,
          skippedDuplicateCount,
          errorMessage,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
          description: Идентификатор import run.
          example: import_run_001
        sourceId:
          type: string
          description: Идентификатор content source, для которого выполнялся импорт.
          example: source_telegram_001
        status:
          $ref: '#/components/schemas/ImportRunStatus'
        startedAt:
          type: string
          format: date-time
          nullable: true
          description: Время начала фактической обработки. Для queued run может быть `null`.
          example: '2026-05-26T05:00:00.000Z'
        finishedAt:
          type: string
          format: date-time
          nullable: true
          description: Время завершения успешной или failed попытки.
          example: '2026-05-26T05:01:00.000Z'
        foundCount:
          type: integer
          minimum: 0
          description: Сколько материалов адаптер обнаружил во внешнем источнике.
          example: 7
        createdCount:
          type: integer
          minimum: 0
          description: Сколько новых материалов создано в библиотеке.
          example: 2
        updatedCount:
          type: integer
          minimum: 0
          description: Сколько существующих материалов обновлено.
          example: 4
        skippedDuplicateCount:
          type: integer
          minimum: 0
          description: Сколько найденных материалов пропущено как дубликаты.
          example: 1
        errorMessage:
          type: string
          nullable: true
          description: Безопасная однострочная диагностика failed run без stack trace и secret-значений.
          example: null
        createdAt:
          type: string
          format: date-time
          description: Время создания записи.
          example: '2026-05-26T05:00:00.000Z'
        updatedAt:
          type: string
          format: date-time
          description: Время последнего обновления записи.
          example: '2026-05-26T05:01:00.000Z'

    ImportRunListResponse:
      type: object
      description: Ограниченный административный список попыток импорта.
      required: [items]
      properties:
        items:
          type: array
          description: Import runs в порядке от новых к старым.
          items:
            $ref: '#/components/schemas/ImportRun'

    PlaceDetail:
      type: object
      description: Детальная карточка места с публично-безопасным pinned material без исходного внешнего URL и счетчиками по платформам.
      required:
        [
          id,
          slug,
          title,
          summary,
          tags,
          category,
          status,
          coverImageUrl,
          mapsUrl,
          counters,
          pinnedMaterial,
        ]
      properties:
        id:
          type: string
          description: Идентификатор места.
          example: place_ekb_001
        slug:
          type: string
          description: Публичный slug места.
          example: baden-baden-uktus
        title:
          type: string
          description: Название места.
          example: Bаден-Баден Уктус
        summary:
          type: string
          description: Короткое описание для каталога.
          example: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          type: array
          description: Набор тегов для поиска и фильтрации.
          items:
            type: string
          example:
            - термы
            - spa
            - бассейн
        category:
          $ref: '#/components/schemas/PlaceCategory'
        status:
          $ref: '#/components/schemas/PlaceStatus'
        coverImageUrl:
          type: string
          nullable: true
          description: Публичный cover-фото места. Если фото отсутствует или не должно отдаться публично, возвращается `null`.
          example: /v1/places/baden-baden-uktus/photo
        mapsUrl:
          type: string
          format: uri
          nullable: true
          description: Canonical URL карточки Яндекс Карт, если место создано через импорт.
          example: https://yandex.ru/maps/org/baden_baden/123456789
        counters:
          type: object
          description: Количество материалов по платформам.
          required: [dzen, telegram, instagram]
          properties:
            dzen:
              type: integer
              example: 12
            telegram:
              type: integer
              example: 7
            instagram:
              type: integer
              example: 3
        pinnedMaterial:
          nullable: true
          description: Закреплённый материал места, если он назначен. Исходный внешний URL не отдается; клиенты должны использовать только `redirectUrl`, когда он доступен.
          oneOf:
            - $ref: '#/components/schemas/PublicMaterial'

    AdminPlaceDetail:
      allOf:
        - $ref: '#/components/schemas/PlaceDetail'
      description: >
        Детальная карточка места для администратора. Shape совпадает с
        `PlaceDetail`: исходный внешний URL закрепленного материала не
        отдается.

    PlaceListResponse:
      type: object
      description: Пагинированный список мест.
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          description: Элементы текущей страницы.
          items:
            $ref: '#/components/schemas/PlaceSummary'
        total:
          type: integer
          description: Общее количество доступных элементов.
          example: 2
        page:
          type: integer
          description: Текущая страница.
          example: 1
        pageSize:
          type: integer
          description: Размер страницы.
          example: 20

    AdminPlaceListResponse:
      type: object
      description: Административный пагинированный список мест с nullable Yandex Maps URL.
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          description: Элементы текущей страницы.
          items:
            $ref: '#/components/schemas/AdminPlaceSummary'
        total:
          type: integer
          description: Общее количество доступных элементов.
          example: 2
        page:
          type: integer
          description: Текущая страница.
          example: 1
        pageSize:
          type: integer
          description: Размер страницы.
          example: 20

    PublicPlaceListResponse:
      type: object
      description: Публичный пагинированный список мест со счетчиками материалов.
      required: [items, total, page, pageSize]
      properties:
        items:
          type: array
          description: Элементы текущей страницы.
          items:
            $ref: '#/components/schemas/PublicPlaceSummary'
        total:
          type: integer
          description: Общее количество доступных элементов.
          example: 2
        page:
          type: integer
          description: Текущая страница.
          example: 1
        pageSize:
          type: integer
          description: Размер страницы.
          example: 20

    MaterialListResponse:
      type: object
      description: Ограниченный список материалов места без исходного внешнего URL.
      required: [items]
      properties:
        items:
          type: array
          description: Материалы места в стабильном порядке отображения.
          items:
            $ref: '#/components/schemas/PublicMaterial'

    CreatePlaceRequest:
      type: object
      description: Payload создания нового места.
      required: [title, categoryId]
      properties:
        slug:
          type: string
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
          description: Необязательный ручной slug. Если поле отсутствует, backend генерирует slug из title.
          example: baden-baden-uktus
        title:
          type: string
          description: Название места.
          example: Bаден-Баден Уктус
        summary:
          type: string
          description: Короткое описание места. Если поле не передано, backend сохранит пустую строку.
          example: Термальный комплекс с открытыми бассейнами и SPA-зоной.
        tags:
          type: array
          description: Теги для поиска и фильтрации. Если поле не передано, backend сохранит пустой массив.
          items:
            type: string
          example:
            - термы
            - spa
            - бассейн
        categoryId:
          type: string
          description: Идентификатор существующей категории места.
          example: category_spa

    UpdatePlaceRequest:
      type: object
      description: Payload частичного обновления места.
      properties:
        slug:
          type: string
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
          description: Новый публичный slug места.
          example: baden-baden-uktus-premium
        title:
          type: string
          description: Новое название места.
          example: Bаден-Баден Уктус Premium
        summary:
          type: string
          description: Обновлённое краткое описание.
          example: Обновлённое описание места для карточки.
        tags:
          type: array
          description: Новый набор тегов.
          items:
            type: string
          example:
            - термы
            - premium
        categoryId:
          type: string
          description: Новый идентификатор существующей категории места.
          example: category_spa

    CreatePlaceCategoryRequest:
      type: object
      description: Payload создания категории места.
      required: [title]
      properties:
        slug:
          type: string
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
          description: Необязательный ручной slug категории из lowercase букв, цифр и одиночных дефисов. Если поле отсутствует, backend генерирует slug из `title`.
          example: family-spa
        title:
          type: string
          description: Название категории.
          example: Family SPA

    UpdatePlaceCategoryRequest:
      type: object
      description: Payload частичного обновления категории места.
      properties:
        slug:
          type: string
          pattern: '^[a-z0-9]+(?:-[a-z0-9]+)*$'
          description: Новый slug категории.
          example: family-spa
        title:
          type: string
          description: Новое название категории.
          example: Family SPA

    UpdatePlaceStatusRequest:
      type: object
      description: Payload изменения статуса места.
      required: [status]
      properties:
        status:
          $ref: '#/components/schemas/PlaceStatus'

    StartPlaceImportRequest:
      type: object
      additionalProperties: false
      description: Ссылка на одну карточку организации Яндекс Карт.
      required: [url]
      properties:
        url:
          type: string
          minLength: 1
          maxLength: 2048
          format: uri
          example: https://yandex.ru/maps/org/aqua_city/123456789

    PlaceImportStatus:
      type: string
      enum:
        [queued, parsing, awaiting_captcha, preview_ready, completed, failed, expired, cancelled]

    PlaceImportCategoryResolution:
      type: string
      enum: [existing, will_create, created]

    PlaceImportOutcome:
      type: string
      nullable: true
      enum: [created, already_exists]

    PlaceImportErrorCode:
      type: string
      enum:
        [
          invalid_url,
          not_organization_url,
          redirect_not_allowed,
          source_not_found,
          captcha_session_expired,
          source_blocked,
          source_timeout,
          parse_failed,
          missing_title,
          missing_primary_category,
          missing_organization_id,
          category_conflict,
          external_identity_conflict,
          confirmation_expired,
          internal_error,
        ]

    PlaceImportCategoryPreview:
      type: object
      required: [id, title, status, resolution]
      properties:
        id:
          type: string
          nullable: true
        title:
          type: string
        status:
          type: string
          nullable: true
          enum: [draft, active]
        resolution:
          $ref: '#/components/schemas/PlaceImportCategoryResolution'

    PlaceImportPossibleDuplicate:
      type: object
      required: [placeId, title]
      properties:
        placeId:
          type: string
        title:
          type: string

    PlaceImportError:
      type: object
      required: [code, message]
      properties:
        code:
          $ref: '#/components/schemas/PlaceImportErrorCode'
        message:
          type: string
          description: Безопасная диагностика без URL query, HTML, cookies, screenshot/HAR и browser state.

    PlaceImportOperation:
      type: object
      description: Read-only snapshot; preview-поля нельзя подменить при confirm.
      required:
        [
          id,
          status,
          version,
          attempt,
          sourceUrl,
          title,
          mapsUrl,
          organizationId,
          category,
          possibleDuplicate,
          captchaExpiresAt,
          previewExpiresAt,
          outcome,
          resultPlaceId,
          error,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
        status:
          $ref: '#/components/schemas/PlaceImportStatus'
        version:
          type: integer
          minimum: 1
        attempt:
          type: integer
          minimum: 0
        sourceUrl:
          type: string
          format: uri
          description: Sanitized URL без credentials, fragment и произвольных query-параметров.
        title:
          type: string
          nullable: true
        mapsUrl:
          type: string
          format: uri
          nullable: true
        organizationId:
          type: string
          nullable: true
        category:
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/PlaceImportCategoryPreview'
        possibleDuplicate:
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/PlaceImportPossibleDuplicate'
        captchaExpiresAt:
          type: string
          format: date-time
          nullable: true
        previewExpiresAt:
          type: string
          format: date-time
          nullable: true
        outcome:
          $ref: '#/components/schemas/PlaceImportOutcome'
        resultPlaceId:
          type: string
          nullable: true
        error:
          nullable: true
          oneOf:
            - $ref: '#/components/schemas/PlaceImportError'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    PlaceImportEvent:
      type: object
      required: [id, operationId, seq, type, version, status, createdAt]
      properties:
        id:
          type: string
        operationId:
          type: string
        seq:
          type: integer
          minimum: 1
        type:
          type: string
        version:
          type: integer
          minimum: 1
        status:
          $ref: '#/components/schemas/PlaceImportStatus'
        createdAt:
          type: string
          format: date-time

    PlaceImportEventsResponse:
      type: object
      required: [operation, events]
      properties:
        operation:
          $ref: '#/components/schemas/PlaceImportOperation'
        events:
          type: array
          items:
            $ref: '#/components/schemas/PlaceImportEvent'

    PlaceImportViewerAccess:
      type: object
      required: [viewerUrl, expiresAt]
      properties:
        viewerUrl:
          type: string
          format: uri
          description: URL отдельного viewer-origin; one-time capability находится только во fragment.
        expiresAt:
          type: string
          format: date-time

    CreateContentSourceRequest:
      type: object
      description: Payload создания пользовательского content source.
      required: [platform, displayName, url]
      properties:
        platform:
          $ref: '#/components/schemas/ContentSourcePlatform'
        displayName:
          type: string
          description: Административное имя источника.
          example: Amazing EKB Telegram
        url:
          type: string
          format: uri
          description: Публичный URL источника. Допускаются только абсолютные http/https URL.
          example: https://t.me/amazing_ekb
        externalId:
          type: string
          nullable: true
          description: Платформенный идентификатор источника, если известен.
          example: '123456'
        handle:
          type: string
          nullable: true
          description: Человекочитаемый handle источника, если он есть.
          example: amazing_ekb
        channelId:
          type: string
          nullable: true
          description: Дополнительный channel id для платформ, где он отличается от externalId/handle.
          example: null

    UpdateContentSourceRequest:
      type: object
      description: Payload частичного обновления content source. `platform`, `lastImportedAt` и `lastCursor` здесь не редактируются. После старта импортов identity-поля `url`, `externalId`, `handle` и `channelId` заблокированы, чтобы не смешивать cursor и imported-material dedupe разных источников.
      properties:
        displayName:
          type: string
          description: Новое административное имя источника.
          example: Amazing EKB Telegram Updated
        url:
          type: string
          format: uri
          description: Новый публичный URL источника. Допускаются только абсолютные http/https URL.
          example: https://t.me/amazing_ekb
        externalId:
          type: string
          nullable: true
          description: Новый платформенный идентификатор или `null`, чтобы очистить поле.
          example: null
        handle:
          type: string
          nullable: true
          description: Новый handle или `null`, чтобы очистить поле.
          example: amazing_ekb
        channelId:
          type: string
          nullable: true
          description: Новый channel id или `null`, чтобы очистить поле.
          example: null

    UpdateContentSourceStatusRequest:
      type: object
      description: Payload переключения статуса content source без удаления записи.
      required: [status]
      properties:
        status:
          $ref: '#/components/schemas/ContentSourceStatus'

    CreateMaterialRequest:
      type: object
      description: Payload создания нового материала для места.
      required: [platform, type, title, publishedAt, url]
      properties:
        platform:
          $ref: '#/components/schemas/Platform'
        type:
          $ref: '#/components/schemas/MaterialType'
        title:
          type: string
          description: Заголовок материала.
          example: Обзор комплекса и советы по посещению
        publishedAt:
          type: string
          format: date
          description: Календарная дата публикации материала в формате `YYYY-MM-DD`.
          example: '2026-03-20'
        durationSec:
          type: integer
          nullable: true
          description: Длительность в секундах для видеоформатов.
          example: 183
        url:
          type: string
          format: uri
          description: Публичная ссылка на материал. Допускаются только абсолютные http/https URL.
          example: https://t.me/amazing_ekb/321

    UpdateMaterialRequest:
      type: object
      description: Payload частичного обновления материала.
      properties:
        platform:
          $ref: '#/components/schemas/Platform'
        type:
          $ref: '#/components/schemas/MaterialType'
        title:
          type: string
          description: Новый заголовок материала.
          example: Обновлённый обзор комплекса
        publishedAt:
          type: string
          format: date
          description: Новая календарная дата публикации в формате `YYYY-MM-DD`.
          example: '2026-03-22'
        durationSec:
          type: integer
          nullable: true
          description: Новая длительность в секундах.
          example: 240
        url:
          type: string
          format: uri
          description: Новая публичная ссылка на материал. Допускаются только абсолютные http/https URL.
          example: https://t.me/amazing_ekb/400

    UpdateMaterialAdminStatusRequest:
      type: object
      description: Payload обновления review-статуса материала в административной библиотеке.
      required: [adminStatus]
      properties:
        adminStatus:
          $ref: '#/components/schemas/MaterialAdminStatus'

    UpdatePlaceMaterialLinkRequest:
      type: object
      description: Payload обновления активной связи материала с местом.
      properties:
        isPinned:
          type: boolean
          description: Нужно ли закрепить материал в блоке “Начни отсюда”. Если `true`, backend снимет закрепление с других материалов этого места.
          example: true
        sortOrder:
          type: integer
          minimum: 0
          description: Ручной порядок отображения в списках материалов места.
          example: 10

    SetPinnedMaterialRequest:
      type: object
      description: Payload назначения закреплённого материала для места.
      required: [materialId]
      properties:
        materialId:
          type: string
          description: Идентификатор материала, который нужно закрепить.
          example: material_telegram_001

    PlacePhotoUploadRequest:
      type: object
      description: Multipart payload для загрузки cover-фото места.
      required: [photo]
      properties:
        photo:
          type: string
          format: binary
          description: Файл cover-фото в формате JPEG, PNG или WebP размеров до 5 MB.

    HealthLiveResponse:
      type: object
      description: Ответ liveness-пробы. Показывает, что процесс приложения запущен.
      required: [status]
      properties:
        status:
          type: string
          description: Признак живости приложения.
          example: ok

    HealthReadyResponse:
      type: object
      description: Ответ readiness-пробы. Показывает готовность приложения обслуживать запросы.
      required: [status, checks]
      properties:
        status:
          type: string
          description: Общий статус готовности.
          example: ok
        checks:
          type: object
          description: Результаты инфраструктурных проверок.
          required: [database]
          properties:
            database:
              type: string
              description: Статус подключения к базе данных.
              example: up

paths:
  /health/live:
    get:
      tags: [Health]
      summary: Liveness probe
      description: Проверяет, что процесс приложения запущен и HTTP-слой отвечает на запросы.
      operationId: getLiveness
      responses:
        '200':
          description: Приложение живо и принимает запросы.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthLiveResponse'
              examples:
                default:
                  $ref: '#/components/examples/HealthLiveResponse'

  /health/ready:
    get:
      tags: [Health]
      summary: Readiness probe
      description: Проверяет готовность приложения обслуживать запросы, включая доступность базы данных.
      operationId: getReadiness
      responses:
        '200':
          description: Приложение готово обслуживать запросы.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthReadyResponse'
              examples:
                default:
                  $ref: '#/components/examples/HealthReadyResponse'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /auth/login:
    post:
      tags: [Auth]
      summary: Login
      description: Аутентифицирует пользователя по email и паролю, ставит access/refresh HttpOnly cookies и возвращает публичный профиль.
      operationId: login
      requestBody:
        required: true
        description: Учетные данные пользователя.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AuthLoginRequest'
            examples:
              default:
                $ref: '#/components/examples/AuthLoginRequest'
      responses:
        '200':
          description: Успешная аутентификация. Ответ устанавливает `aeh_access_token` и `aeh_refresh_token`.
          headers:
            Set-Cookie:
              description: HttpOnly auth cookies для access и refresh токенов.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthMeResponse'
              examples:
                default:
                  $ref: '#/components/examples/AuthMeResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh tokens
      description: Обновляет access/refresh HttpOnly cookies по валидной refresh cookie.
      operationId: refreshTokens
      security:
        - RefreshCookieAuth: []
      responses:
        '204':
          description: Cookies успешно перевыпущены.
          headers:
            Set-Cookie:
              description: Новые HttpOnly auth cookies для access и refresh токенов.
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/TooManyRequests'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Logout
      description: Выполняет logout, отзывает refresh token из cookie и очищает auth cookies.
      operationId: logout
      security:
        - RefreshCookieAuth: []
      responses:
        '204':
          description: Refresh token отозван, auth cookies очищены.
          headers:
            Set-Cookie:
              description: Очистка `aeh_access_token` и `aeh_refresh_token`.
              schema:
                type: string

  /auth/me:
    get:
      tags: [Auth]
      summary: Get current user
      description: Возвращает публичный профиль текущего аутентифицированного пользователя.
      operationId: getCurrentUser
      security:
        - AccessCookieAuth: []
      responses:
        '200':
          description: Профиль текущего пользователя.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthMeResponse'
              examples:
                default:
                  $ref: '#/components/examples/AuthMeResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /categories:
    get:
      tags: [Places]
      summary: List place categories
      description: Возвращает публичный справочник категорий мест для фильтров и бейджей.
      operationId: listPlaceCategories
      responses:
        '200':
          description: Список категорий мест.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceCategoryListResponse'
              examples:
                default:
                  $ref: '#/components/examples/PlaceCategoryListResponse'

  /categories/{categorySlug}:
    get:
      tags: [Places]
      summary: Get place category
      description: Возвращает публичную категорию места по её slug.
      operationId: getPlaceCategory
      parameters:
        - $ref: '#/components/parameters/CategorySlug'
      responses:
        '200':
          description: Категория места.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceCategory'
              examples:
                default:
                  $ref: '#/components/examples/PlaceCategory'
        '404':
          $ref: '#/components/responses/CategoryNotFound'

  /categories/{categorySlug}/photo:
    get:
      tags: [Places]
      summary: Get place category photo
      description: Возвращает бинарную cover-фотографию категории с сохранённым MIME-типом. Неизвестная категория, пустые metadata и отсутствующий файл возвращают 404.
      operationId: getPlaceCategoryPhoto
      parameters:
        - $ref: '#/components/parameters/CategorySlug'
      responses:
        '200':
          description: Бинарное содержимое cover-фотографии категории.
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/CategoryNotFound'

  /places:
    get:
      tags: [Places]
      summary: List places
      description: Возвращает публичный список мест с пагинацией, поиском и фильтрацией по категории в стабильном порядке `title ASC, id ASC`.
      operationId: listPlaces
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - name: search
          in: query
          required: false
          description: Полнотекстовый поиск по названию и описанию места. Максимум 100 символов.
          schema:
            type: string
            maxLength: 100
          example: термы
        - name: categoryId
          in: query
          required: false
          description: Фильтр по идентификатору категории места.
          schema:
            type: string
          example: category_spa
      responses:
        '200':
          description: Пагинированный список мест.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicPlaceListResponse'
              examples:
                default:
                  $ref: '#/components/examples/PublicPlaceListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'

  /places/{placeSlug}:
    get:
      tags: [Places]
      summary: Get place details
      description: Возвращает детальную карточку публичного места по его slug.
      operationId: getPlaceDetail
      parameters:
        - $ref: '#/components/parameters/PlaceSlug'
      responses:
        '200':
          description: Детальная карточка места.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceDetail'
              examples:
                default:
                  $ref: '#/components/examples/PlaceDetail'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /places/{placeSlug}/photo:
    get:
      tags: [Places]
      summary: Get place cover photo
      description: Возвращает бинарное содержимое публичного cover-фото активного места.
      operationId: getPlaceCoverPhoto
      parameters:
        - $ref: '#/components/parameters/PlaceSlug'
      responses:
        '200':
          description: Бинарное содержимое публичного cover-фото.
          content:
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /places/{placeSlug}/materials:
    get:
      tags: [Places]
      summary: List place materials
      description: Возвращает до 100 материалов, связанных с указанным активным местом, с опциональным фильтром по платформе.
      operationId: listPlaceMaterials
      parameters:
        - $ref: '#/components/parameters/PlaceSlug'
        - name: platform
          in: query
          required: false
          description: Фильтр по платформе публикации материала.
          schema:
            $ref: '#/components/schemas/Platform'
          example: telegram
      responses:
        '200':
          description: Список материалов места.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaterialListResponse'
              examples:
                default:
                  $ref: '#/components/examples/MaterialListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /materials/{materialId}/go:
    get:
      tags: [Places]
      summary: Redirect to material URL
      description: Выполняет временный redirect на сохраненный URL публичного материала. Endpoint не принимает внешний URL от клиента и работает только для материалов активных мест с безопасной https-ссылкой платформы.
      operationId: redirectMaterial
      parameters:
        - $ref: '#/components/parameters/MaterialId'
      responses:
        '302':
          description: Временный redirect на сохраненный внешний URL материала.
          headers:
            Location:
              description: Безопасный абсолютный https URL материала.
              schema:
                type: string
                format: uri
        '404':
          $ref: '#/components/responses/MaterialNotFound'

  /favorites:
    get:
      tags: [Favorites]
      summary: List favorites
      description: Возвращает список избранных мест текущего аутентифицированного пользователя.
      operationId: listFavorites
      security:
        - AccessCookieAuth: []
      responses:
        '200':
          description: Список избранных мест.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceListResponse'
              examples:
                default:
                  $ref: '#/components/examples/PlaceListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /favorites/{placeId}:
    post:
      tags: [Favorites]
      summary: Add favorite
      description: Добавляет место в избранное текущего пользователя.
      operationId: addFavorite
      security:
        - AccessCookieAuth: []
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '204':
          description: Место добавлено в избранное.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/PlaceNotFound'
    delete:
      tags: [Favorites]
      summary: Remove favorite
      description: Удаляет место из избранного текущего пользователя.
      operationId: removeFavorite
      security:
        - AccessCookieAuth: []
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '204':
          description: Место удалено из избранного.
        '401':
          $ref: '#/components/responses/Unauthorized'

  /admin/categories:
    get:
      tags: [Admin]
      summary: List admin place categories
      description: Возвращает административный справочник категорий мест.
      operationId: listAdminPlaceCategories
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      responses:
        '200':
          description: Список категорий мест.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceCategoryListResponse'
              examples:
                default:
                  $ref: '#/components/examples/AdminPlaceCategoryListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [Admin]
      summary: Create place category
      description: Создает категорию места. Операция доступна только администратору.
      operationId: createPlaceCategory
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      requestBody:
        required: true
        description: Данные новой категории.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlaceCategoryRequest'
            examples:
              default:
                value:
                  title: Family SPA
      responses:
        '201':
          description: Категория создана.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceCategory'
              examples:
                default:
                  $ref: '#/components/examples/AdminPlaceCategory'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/CategoryConflict'

  /admin/categories/{categoryId}:
    get:
      tags: [Admin]
      summary: Get admin place category
      description: Возвращает категорию места по идентификатору.
      operationId: getAdminPlaceCategory
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/CategoryId'
      responses:
        '200':
          description: Категория найдена.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceCategory'
              examples:
                default:
                  $ref: '#/components/examples/AdminPlaceCategory'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/CategoryNotFound'
    patch:
      tags: [Admin]
      summary: Update place category
      description: Частично обновляет категорию места.
      operationId: updatePlaceCategory
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/CategoryId'
      requestBody:
        required: true
        description: Поля категории для обновления.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlaceCategoryRequest'
            examples:
              default:
                value:
                  title: Family SPA
      responses:
        '200':
          description: Категория обновлена.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceCategory'
              examples:
                default:
                  $ref: '#/components/examples/AdminPlaceCategory'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/CategoryNotFound'
        '409':
          $ref: '#/components/responses/CategoryConflict'
    delete:
      tags: [Admin]
      summary: Delete place category
      description: Удаляет категорию места, если она не используется местами.
      operationId: deletePlaceCategory
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/CategoryId'
      responses:
        '204':
          description: Категория удалена.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/CategoryNotFound'
        '409':
          $ref: '#/components/responses/CategoryConflict'

  /admin/categories/{categoryId}/photo:
    post:
      tags: [Admin]
      summary: Upload place category photo
      description: Загружает или заменяет cover-фотографию категории. Принимаются JPEG, PNG и WebP размером не более 5 MB.
      operationId: uploadPlaceCategoryPhoto
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/CategoryId'
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [photo]
              properties:
                photo:
                  type: string
                  format: binary
                  description: JPEG, PNG или WebP файл размером не более 5 MB.
      responses:
        '200':
          description: Фотография загружена, category metadata и versioned URL обновлены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceCategory'
              examples:
                default:
                  $ref: '#/components/examples/AdminPlaceCategory'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/CategoryNotFound'

  /admin/place-imports/yandex-maps:
    post:
      tags: [Admin]
      summary: Start Yandex Maps place import
      description: |
        Создает durable queued operation для одной карточки организации. URL проверяется и очищается до persistence. Endpoint требует trusted Origin/Referer; feature по умолчанию выключена.
      operationId: startYandexMapsPlaceImport
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartPlaceImportRequest'
      responses:
        '202':
          description: Operation committed; delivery может быть повторена dispatcher-ом.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceImportOperation'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: У администратора уже есть активная operation.
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /admin/place-imports/{operationId}:
    get:
      tags: [Admin]
      summary: Get place import operation
      operationId: getPlaceImportOperation
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
      responses:
        '200':
          description: Текущий source-of-truth snapshot.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceImportOperation'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Operation не найдена или принадлежит другому пользователю.
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /admin/place-imports/{operationId}/events:
    get:
      tags: [Admin]
      summary: Read place import journal
      description: Polling fallback и reconnect delta после известной operation version.
      operationId: readPlaceImportEvents
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
        - name: afterVersion
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: Snapshot и append-only journal delta.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceImportEventsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Operation не найдена.

  /admin/place-imports/{operationId}/events/stream:
    get:
      tags: [Admin]
      summary: Stream place import updates
      description: |
        SSE `place-import.updated` использует subscribe → read → read handshake, Redis version hints и периодическое PostgreSQL reconciliation. Terminal status закрывает stream.
      operationId: streamPlaceImportEvents
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
        - name: afterVersion
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        '200':
          description: SSE stream; `data` соответствует PlaceImportEventsResponse.
          content:
            text/event-stream:
              schema:
                type: string
              example: |
                id: 3
                event: place-import.updated
                data: {"operation":{"id":"cmrvo_place_import_001","status":"preview_ready","version":3},"events":[]}
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/place-imports/{operationId}/confirm:
    post:
      tags: [Admin]
      summary: Confirm immutable place import preview
      description: Без request body атомарно создает hidden Place, external reference и при необходимости draft-категорию. Требует trusted Origin/Referer.
      operationId: confirmPlaceImport
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
      responses:
        '201':
          description: Создано hidden место либо возвращено строго существующее.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceImportOperation'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Operation не найдена.
        '409':
          description: Preview не готов или обнаружен external identity conflict.
        '410':
          description: Preview TTL истёк.
        '503':
          $ref: '#/components/responses/ServiceUnavailable'

  /admin/place-imports/{operationId}/cancel:
    post:
      tags: [Admin]
      summary: Cancel place import
      description: Durable cancellation; Redis notification является только latency hint. Требует trusted Origin/Referer.
      operationId: cancelPlaceImport
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
      responses:
        '201':
          description: Cancelled или ранее terminal snapshot.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceImportOperation'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Operation не найдена.

  /admin/place-imports/{operationId}/viewer-access:
    post:
      tags: [Admin]
      summary: Create one-time CAPTCHA viewer access
      description: Выдает один capability во fragment отдельного viewer-origin. Повторная выдача блокируется до revoke/expiry.
      operationId: createPlaceImportViewerAccess
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
      responses:
        '201':
          description: Viewer capability создан.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceImportViewerAccess'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: CAPTCHA state/solver lease не допускает выдачу.
    delete:
      tags: [Admin]
      summary: Revoke CAPTCHA viewer access
      operationId: revokePlaceImportViewerAccess
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceImportOperationId'
      responses:
        '204':
          description: Capability/session удалены, старый WebSocket закрывается.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Активный solver lease отсутствует.

  /admin/places:
    get:
      tags: [Admin]
      summary: List admin places
      description: Возвращает административный список мест с пагинацией и опциональной фильтрацией по статусу. Если `status` не указан, возвращаются и активные, и скрытые места.
      operationId: listAdminPlaces
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
        - name: status
          in: query
          required: false
          description: Фильтр по статусу места. Если параметр отсутствует, возвращаются все статусы.
          schema:
            $ref: '#/components/schemas/PlaceStatus'
          example: hidden
      responses:
        '200':
          description: Пагинированный административный список мест.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceListResponse'
              examples:
                default:
                  $ref: '#/components/examples/AdminPlaceListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    post:
      tags: [Admin]
      summary: Create place
      description: Создаёт новое место в каталоге. Операция доступна только администратору.
      operationId: createPlace
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      requestBody:
        required: true
        description: Данные нового места.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePlaceRequest'
            examples:
              default:
                value:
                  title: Bаден-Баден Уктус
                  summary: Термальный комплекс с открытыми бассейнами и SPA-зоной.
                  tags:
                    - термы
                    - spa
                    - бассейн
                  categoryId: category_spa
                  slug: baden-baden-uktus
      responses:
        '201':
          description: Место успешно создано.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceSummary'
              examples:
                default:
                  $ref: '#/components/examples/PlaceSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/CategoryNotFound'

  /admin/places/{placeId}:
    get:
      tags: [Admin]
      summary: Get admin place details
      description: Возвращает детальную карточку места для администратора независимо от публичного статуса места.
      operationId: getAdminPlaceDetail
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '200':
          description: Детальная карточка места для администратора.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceDetail'
              examples:
                default:
                  $ref: '#/components/examples/PlaceDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/PlaceNotFound'
    patch:
      tags: [Admin]
      summary: Update place
      description: Частично обновляет место по идентификатору. Операция доступна только администратору.
      operationId: updatePlace
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      requestBody:
        required: true
        description: Набор полей для частичного обновления места.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlaceRequest'
            examples:
              default:
                value:
                  title: Bаден-Баден Уктус Premium
                  summary: Обновлённое описание места для карточки.
                  tags:
                    - термы
                    - premium
                  categoryId: category_spa
                  slug: baden-baden-uktus-premium
      responses:
        '200':
          description: Место успешно обновлено.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceSummary'
              examples:
                default:
                  $ref: '#/components/examples/PlaceSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Место или категория не найдены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                place:
                  $ref: '#/components/examples/PlaceNotFoundError'
                category:
                  $ref: '#/components/examples/CategoryNotFoundError'

  /admin/places/{placeId}/status:
    patch:
      tags: [Admin]
      summary: Update place status
      description: Меняет статус публикации места. Операция доступна только администратору.
      operationId: updatePlaceStatus
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      requestBody:
        required: true
        description: Новый статус места.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlaceStatusRequest'
            examples:
              default:
                value:
                  status: hidden
      responses:
        '200':
          description: Статус места успешно обновлён.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceSummary'
              examples:
                default:
                  $ref: '#/components/examples/PlaceSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /admin/places/{placeId}/photo:
    post:
      tags: [Admin]
      summary: Upload place cover photo
      description: Загружает или заменяет cover-фото места. Операция доступна только администратору.
      operationId: uploadPlaceCoverPhoto
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      requestBody:
        required: true
        description: Multipart payload с одиночным файлом в поле `photo`.
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PlacePhotoUploadRequest'
            encoding:
              photo:
                contentType: image/jpeg, image/png, image/webp
      responses:
        '200':
          description: Cover-фото места успешно загружено или заменено.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PlaceSummary'
              examples:
                default:
                  $ref: '#/components/examples/PlaceSummary'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /admin/content-sources:
    get:
      tags: [Admin]
      summary: List admin content sources
      description: Возвращает до 100 пользовательских content sources. Источник — это управляемый канал/ресурс пользователя, а не случайная внешняя ссылка.
      operationId: listContentSources
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - name: platform
          in: query
          required: false
          description: Фильтр по платформе источника.
          schema:
            $ref: '#/components/schemas/ContentSourcePlatform'
          example: telegram
        - name: status
          in: query
          required: false
          description: Фильтр по статусу источника.
          schema:
            $ref: '#/components/schemas/ContentSourceStatus'
          example: active
      responses:
        '200':
          description: Список content sources.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentSourceListResponse'
              examples:
                default:
                  $ref: '#/components/examples/ContentSourceListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

    post:
      tags: [Admin]
      summary: Create content source
      description: Создает пользовательский content source для будущих импортов материалов.
      operationId: createContentSource
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      requestBody:
        required: true
        description: Данные нового content source.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateContentSourceRequest'
            examples:
              default:
                value:
                  platform: telegram
                  displayName: Amazing EKB Telegram
                  url: https://t.me/amazing_ekb
                  externalId: '123456'
                  handle: amazing_ekb
                  channelId: null
      responses:
        '201':
          description: Content source создан.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentSource'
              examples:
                default:
                  $ref: '#/components/examples/ContentSource'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          $ref: '#/components/responses/ContentSourceConflict'

  /admin/content-sources/{sourceId}:
    patch:
      tags: [Admin]
      summary: Update content source
      description: Частично обновляет content source. Platform и import cursor-поля в этом endpoint не изменяются.
      operationId: updateContentSource
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/ContentSourceId'
      requestBody:
        required: true
        description: Поля content source, которые нужно обновить.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateContentSourceRequest'
            examples:
              default:
                value:
                  displayName: Amazing EKB Telegram Updated
                  externalId: null
      responses:
        '200':
          description: Content source обновлен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentSource'
              examples:
                default:
                  $ref: '#/components/examples/ContentSource'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/ContentSourceNotFound'
        '409':
          $ref: '#/components/responses/ContentSourceConflict'

  /admin/content-sources/{sourceId}/status:
    patch:
      tags: [Admin]
      summary: Update content source status
      description: Переключает content source между `active` и `disabled` без удаления записи.
      operationId: updateContentSourceStatus
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/ContentSourceId'
      requestBody:
        required: true
        description: Новый статус content source.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateContentSourceStatusRequest'
            examples:
              default:
                value:
                  status: disabled
      responses:
        '200':
          description: Статус content source обновлен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContentSource'
              examples:
                default:
                  $ref: '#/components/examples/ContentSource'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/ContentSourceNotFound'

  /admin/import-runs:
    get:
      tags: [Admin]
      summary: List import runs
      description: Возвращает до 100 диагностических записей попыток импорта материалов.
      operationId: listImportRuns
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - name: sourceId
          in: query
          required: false
          description: Фильтр по content source.
          schema:
            type: string
          example: source_telegram_001
        - name: status
          in: query
          required: false
          description: Фильтр по статусу import run.
          schema:
            $ref: '#/components/schemas/ImportRunStatus'
          example: completed
      responses:
        '200':
          description: Список import runs.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportRunListResponse'
              examples:
                default:
                  $ref: '#/components/examples/ImportRunListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/import-runs/{runId}/events:
    get:
      tags: [Admin]
      summary: Stream import run updates
      description: |
        Открывает Server-Sent Events stream для одного import run. Stream сразу отправляет initial snapshot текущего `ImportRun`, затем runtime-обновления `import-run.updated` при изменении статуса или счетчиков.

        Если подписка не может быть подготовлена, NestJS SSE handler отправляет `event: error` и закрывает stream.

        БД и `GET /admin/import-runs` остаются источником истины и fallback для refresh/reconnect; in-memory SSE доставляет только обновления текущего backend process.
      operationId: streamImportRunEvents
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - name: runId
          in: path
          required: true
          description: Идентификатор import run.
          schema:
            type: string
          example: import_run_001
      responses:
        '200':
          description: Server-Sent Events stream. Каждое событие `import-run.updated` содержит JSON-serialized `ImportRun` в `data`; ошибка подготовки подписки приходит как SSE `error` event.
          content:
            text/event-stream:
              schema:
                type: string
                description: SSE stream, где `data` событий `import-run.updated` соответствует `#/components/schemas/ImportRun`.
              examples:
                running:
                  summary: Running snapshot event
                  value: |
                    event: import-run.updated
                    data: {"id":"import_run_001","sourceId":"source_telegram_001","status":"running","startedAt":"2026-05-26T05:00:00.000Z","finishedAt":null,"foundCount":3,"createdCount":2,"updatedCount":1,"skippedDuplicateCount":0,"errorMessage":null,"createdAt":"2026-05-26T05:00:00.000Z","updatedAt":"2026-05-26T05:00:10.000Z"}
                missingRun:
                  summary: Missing run subscription error
                  value: |
                    event: error
                    data: Import run not found
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'

  /admin/content-sources/{sourceId}/imports/telegram:
    post:
      tags: [Admin]
      summary: Import Telegram channel posts
      description: Создает durable queued one-click Telegram import/backfill run для active Telegram content source. HTTP не ждет GramJS/Telegram processing; worker позже переведет run в `running`, обработает до 20 внутренних batch-ов по `limit` логических постов, сохраняя cursor после каждого успешного непустого batch-а, и остановится при исчерпании истории, safety cap или final failure. Fresh active `queued/running` run того же source возвращает `409 Conflict`; stale `queued/running` runs старше консервативного timeout закрываются как `failed` перед созданием новой queued попытки.
      operationId: importTelegramChannel
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/ContentSourceId'
        - name: limit
          in: query
          required: false
          description: Размер одного внутреннего batch-а в логических Telegram-постах; одиночный message считается одним постом, album/media group с общим groupedId тоже считается одним постом. Один queued run может обработать несколько batch-ов подряд и останавливается при исчерпании истории или safety cap 20 batch-ов.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
          example: 50
      responses:
        '201':
          description: Telegram import job поставлен в очередь; response содержит queued import run reference.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportRun'
              examples:
                default:
                  $ref: '#/components/examples/QueuedImportRun'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/ContentSourceNotFound'
        '409':
          $ref: '#/components/responses/TelegramImportAlreadyRunning'
        '503':
          description: Import run создан, но job не удалось поставить в queue; run переведен в failed с безопасной диагностикой.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                default:
                  value:
                    statusCode: 503
                    message: Import queue is unavailable
                    error: Service Unavailable

  /admin/materials:
    get:
      tags: [Admin]
      summary: List admin material library
      description: Возвращает страницу материалов общей библиотеки. Если передать `placeId`, каждый item содержит статус связи с этим местом.
      operationId: listAdminMaterialLibrary
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - name: platform
          in: query
          required: false
          description: Фильтр по платформе публикации материала.
          schema:
            $ref: '#/components/schemas/Platform'
          example: telegram
        - name: placeId
          in: query
          required: false
          description: Идентификатор места, для которого нужно вернуть статус связи `placeLink`.
          schema:
            type: string
          example: place_ekb_001
        - name: adminStatus
          in: query
          required: false
          description: Фильтр по review-статусу материала. Для selector-а привязки обычно используется `approved`.
          schema:
            $ref: '#/components/schemas/MaterialAdminStatus'
          example: approved
        - name: linked
          in: query
          required: false
          description: Фильтр по глобальному наличию `PlaceMaterial` связи. `false` означает, что у материала нет ни одной связи с любым place, включая hidden.
          schema:
            type: boolean
          example: false
        - $ref: '#/components/parameters/Page'
        - name: pageSize
          in: query
          required: false
          description: Размер страницы. Допустимый диапазон от `1` до `100`. По умолчанию `100`, чтобы сохранить прежний bounded list размер без явной пагинации.
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 100
          example: 100
      responses:
        '200':
          description: Список материалов общей библиотеки.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminMaterialLibraryListResponse'
              examples:
                default:
                  $ref: '#/components/examples/AdminMaterialLibraryListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /admin/places/{placeId}/materials:
    get:
      tags: [Admin]
      summary: List admin place materials
      description: Возвращает до 100 материалов указанного места для администратора, включая скрытые места.
      operationId: listAdminPlaceMaterials
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
        - name: platform
          in: query
          required: false
          description: Фильтр по платформе публикации материала.
          schema:
            $ref: '#/components/schemas/Platform'
          example: telegram
      responses:
        '200':
          description: Список материалов места для администратора.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaterialListResponse'
              examples:
                default:
                  $ref: '#/components/examples/MaterialListResponse'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

    post:
      tags: [Admin]
      summary: Create material for place
      description: Создаёт новый материал для указанного места. Операция доступна только администратору.
      operationId: createPlaceMaterial
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      requestBody:
        required: true
        description: Данные нового материала.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMaterialRequest'
            examples:
              default:
                value:
                  platform: telegram
                  type: post
                  title: Обзор комплекса и советы по посещению
                  publishedAt: '2026-03-20'
                  durationSec: null
                  url: https://t.me/amazing_ekb/321
      responses:
        '201':
          description: Материал успешно создан.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Material'
              examples:
                default:
                  $ref: '#/components/examples/Material'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/PlaceNotFound'

  /admin/places/{placeId}/materials/{materialId}:
    put:
      tags: [Admin]
      summary: Link material to place
      description: Создает или реактивирует связь существующего библиотечного материала с местом. Повторный active-link запрос идемпотентен.
      operationId: linkPlaceMaterial
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
        - $ref: '#/components/parameters/MaterialId'
      responses:
        '200':
          description: Материал связан с местом.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Material'
              examples:
                default:
                  $ref: '#/components/examples/Material'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Место или материал не найдены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                placeNotFound:
                  $ref: '#/components/examples/PlaceNotFoundError'
                materialNotFound:
                  $ref: '#/components/examples/MaterialNotFoundError'

    patch:
      tags: [Admin]
      summary: Update place-material link
      description: 'Обновляет активную связь материала с местом: закрепление и ручной порядок отображения.'
      operationId: updatePlaceMaterialLink
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
        - $ref: '#/components/parameters/MaterialId'
      requestBody:
        required: true
        description: Поля связи, которые нужно обновить.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePlaceMaterialLinkRequest'
            examples:
              default:
                value:
                  isPinned: true
                  sortOrder: 10
      responses:
        '200':
          description: Связь материала с местом обновлена.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Material'
              examples:
                default:
                  $ref: '#/components/examples/Material'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Место, материал или активная связь не найдены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                placeNotFound:
                  $ref: '#/components/examples/PlaceNotFoundError'
                materialNotFound:
                  $ref: '#/components/examples/MaterialNotFoundError'
                linkNotFound:
                  value:
                    statusCode: 404
                    message: Place material link not found
                    error: Not Found

    delete:
      tags: [Admin]
      summary: Hide place-material link
      description: Скрывает активную связь материала с местом без удаления материала из общей библиотеки.
      operationId: hidePlaceMaterialLink
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
        - $ref: '#/components/parameters/MaterialId'
      responses:
        '204':
          description: Связь скрыта, тело ответа отсутствует.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Место, материал или активная связь не найдены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                placeNotFound:
                  $ref: '#/components/examples/PlaceNotFoundError'
                materialNotFound:
                  $ref: '#/components/examples/MaterialNotFoundError'
                linkNotFound:
                  value:
                    statusCode: 404
                    message: Place material link not found
                    error: Not Found

  /admin/materials/{materialId}:
    patch:
      tags: [Admin]
      summary: Update material
      description: Частично обновляет материал по идентификатору. Операция доступна только администратору.
      operationId: updateMaterial
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/MaterialId'
      requestBody:
        required: true
        description: Набор полей для частичного обновления материала.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMaterialRequest'
            examples:
              default:
                value:
                  platform: dzen
                  type: video
                  title: Обновлённый видеообзор комплекса
                  publishedAt: '2026-03-22'
                  durationSec: 240
                  url: https://dzen.ru/video/watch/abcdef
      responses:
        '200':
          description: Материал успешно обновлён.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Material'
              examples:
                default:
                  $ref: '#/components/examples/Material'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/MaterialNotFound'

  /admin/materials/{materialId}/admin-status:
    patch:
      tags: [Admin]
      summary: Update material admin status
      description: Обновляет review-статус материала в административной библиотеке. Статус не удаляет материал и сам по себе не меняет публичную видимость уже существующих `PlaceMaterial` связей.
      operationId: updateMaterialAdminStatus
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/MaterialId'
      requestBody:
        required: true
        description: Новый review-статус материала.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMaterialAdminStatusRequest'
            examples:
              approve:
                value:
                  adminStatus: approved
              archive:
                value:
                  adminStatus: archived
      responses:
        '200':
          description: Review-статус материала обновлен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminMaterialLibraryItem'
              examples:
                default:
                  value:
                    id: material_telegram_001
                    platform: telegram
                    type: post
                    title: null
                    publishedAt: '2026-03-20'
                    durationSec: null
                    url: https://t.me/amazing_ekb/321
                    source:
                      id: source_telegram_001
                      platform: telegram
                      displayName: Amazing EKB Telegram
                      url: https://t.me/amazing_ekb
                    externalId: '321'
                    text: 'Пост из Telegram-канала Amazing EKB'
                    excerpt: 'Пост из Telegram-канала Amazing EKB'
                    mediaKind: photo
                    mediaPreviewUrl: null
                    adminStatus: approved
                    linked: false
                    placeLink: null
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/MaterialNotFound'

  /admin/places/{placeId}/pinned-material:
    patch:
      tags: [Admin]
      summary: Set pinned material for place
      description: Назначает закреплённый материал для места. Операция доступна только администратору.
      operationId: setPinnedMaterial
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      requestBody:
        required: true
        description: Идентификатор материала, который нужно закрепить за местом.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SetPinnedMaterialRequest'
            examples:
              default:
                value:
                  materialId: material_telegram_001
      responses:
        '200':
          description: Закреплённый материал успешно обновлён.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceDetail'
              examples:
                default:
                  $ref: '#/components/examples/PlaceDetail'
        '400':
          description: Ошибка валидации или нарушение бизнес-правила.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                validation:
                  $ref: '#/components/examples/ValidationError'
                badRequest:
                  $ref: '#/components/examples/PinnedMaterialBadRequestError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Место или материал не найдены.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                placeNotFound:
                  $ref: '#/components/examples/PlaceNotFoundError'
                materialNotFound:
                  $ref: '#/components/examples/MaterialNotFoundError'
    delete:
      tags: [Admin]
      summary: Clear pinned material for place
      description: Снимает закреплённый материал с места. Операция доступна только администратору.
      operationId: clearPinnedMaterial
      security:
        - AccessCookieAuth: []
      x-required-role: admin
      parameters:
        - $ref: '#/components/parameters/PlaceId'
      responses:
        '200':
          description: Закреплённый материал успешно очищен.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPlaceDetail'
              examples:
                default:
                  $ref: '#/components/examples/PlaceDetail'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Место не найдено.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NestErrorResponse'
              examples:
                placeNotFound:
                  $ref: '#/components/examples/PlaceNotFoundError'
