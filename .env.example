# Remote development backend origin for Next.js rewrites during frontend-only development.
# Keep this value server-only: it is read in next.config.ts and must not be exposed via NEXT_PUBLIC_*.
# For full-stack local debugging, replace this with http://127.0.0.1:3000/v1.
API_BASE_URL=https://api.strelchukgo.ru/v1
