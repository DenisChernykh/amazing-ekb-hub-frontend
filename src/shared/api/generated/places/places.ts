/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Amazing EKB Hub API
 * MVP backend API (OpenAPI-first)
 * OpenAPI spec version: 1.0.0
 */
import type {
  GetPlaceCoverPhotoPathParameters,
  GetPlaceDetailPathParameters,
  ListPlaceMaterialsParams,
  ListPlaceMaterialsPathParameters,
  ListPlacesParams,
  MaterialListResponse,
  MaterialNotFoundResponse,
  PlaceDetail,
  PlaceNotFoundResponse,
  PublicPlaceListResponse,
  RedirectMaterialPathParameters,
  ValidationErrorResponse,
} from '../model';

/**
 * Возвращает публичный список мест с пагинацией, поиском и фильтрацией по категории.
 * @summary List places
 */
export type listPlacesResponse200 = {
  data: PublicPlaceListResponse;
  status: 200;
};

export type listPlacesResponse400 = {
  data: ValidationErrorResponse;
  status: 400;
};

export type listPlacesResponseSuccess = listPlacesResponse200 & {
  headers: Headers;
};
export type listPlacesResponseError = listPlacesResponse400 & {
  headers: Headers;
};

export const getListPlacesUrl = (params?: ListPlacesParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `${process.env.API_BASE_URL}/places?${stringifiedParams}`
    : `${process.env.API_BASE_URL}/places`;
};

export const listPlaces = async (
  params?: ListPlacesParams,
  options?: RequestInit,
): Promise<listPlacesResponseSuccess> => {
  const res = await fetch(getListPlacesUrl(params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: listPlacesResponseError['data']; status?: number } =
      new globalThis.Error();
    const data: listPlacesResponseError['data'] = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  const data: listPlacesResponseSuccess['data'] = body ? JSON.parse(body) : {};
  return { data, status: res.status, headers: res.headers } as listPlacesResponseSuccess;
};

/**
 * Возвращает детальную карточку публичного места по его идентификатору.
 * @summary Get place details
 */
export type getPlaceDetailResponse200 = {
  data: PlaceDetail;
  status: 200;
};

export type getPlaceDetailResponse404 = {
  data: PlaceNotFoundResponse;
  status: 404;
};

export type getPlaceDetailResponseSuccess = getPlaceDetailResponse200 & {
  headers: Headers;
};
export type getPlaceDetailResponseError = getPlaceDetailResponse404 & {
  headers: Headers;
};

export const getGetPlaceDetailUrl = ({ placeId }: GetPlaceDetailPathParameters) => {
  return `${process.env.API_BASE_URL}/places/${placeId}`;
};

export const getPlaceDetail = async (
  { placeId }: GetPlaceDetailPathParameters,
  options?: RequestInit,
): Promise<getPlaceDetailResponseSuccess> => {
  const res = await fetch(getGetPlaceDetailUrl({ placeId }), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & { info?: getPlaceDetailResponseError['data']; status?: number } =
      new globalThis.Error();
    const data: getPlaceDetailResponseError['data'] = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  const data: getPlaceDetailResponseSuccess['data'] = body ? JSON.parse(body) : {};
  return { data, status: res.status, headers: res.headers } as getPlaceDetailResponseSuccess;
};

/**
 * Возвращает бинарное содержимое публичного cover-фото активного места.
 * @summary Get place cover photo
 */
export type getPlaceCoverPhotoResponse200ImageJpeg = {
  data: Blob;
  status: 200;
};

export type getPlaceCoverPhotoResponse200ImagePng = {
  data: Blob;
  status: 200;
};

export type getPlaceCoverPhotoResponse200ImageWebp = {
  data: Blob;
  status: 200;
};

export type getPlaceCoverPhotoResponse404 = {
  data: PlaceNotFoundResponse;
  status: 404;
};

export type getPlaceCoverPhotoResponseSuccess = (
  | getPlaceCoverPhotoResponse200ImageJpeg
  | getPlaceCoverPhotoResponse200ImagePng
  | getPlaceCoverPhotoResponse200ImageWebp
) & {
  headers: Headers;
};
export type getPlaceCoverPhotoResponseError = getPlaceCoverPhotoResponse404 & {
  headers: Headers;
};

export const getGetPlaceCoverPhotoUrl = ({ placeId }: GetPlaceCoverPhotoPathParameters) => {
  return `${process.env.API_BASE_URL}/places/${placeId}/photo`;
};

export const getPlaceCoverPhoto = async (
  { placeId }: GetPlaceCoverPhotoPathParameters,
  options?: RequestInit,
): Promise<getPlaceCoverPhotoResponseSuccess> => {
  const res = await fetch(getGetPlaceCoverPhotoUrl({ placeId }), {
    ...options,
    method: 'GET',
  });

  if (!res.ok) {
    const errorBody = [204, 205, 304].includes(res.status) ? null : await res.text();

    const err: globalThis.Error & {
      info?: getPlaceCoverPhotoResponseError['data'];
      status?: number;
    } = new globalThis.Error();
    const data: getPlaceCoverPhotoResponseError['data'] = errorBody ? JSON.parse(errorBody) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  const body = [204, 205, 304].includes(res.status) ? null : await res.blob();
  const data: getPlaceCoverPhotoResponseSuccess['data'] =
    body as getPlaceCoverPhotoResponseSuccess['data'];
  return { data, status: res.status, headers: res.headers } as getPlaceCoverPhotoResponseSuccess;
};

/**
 * Возвращает до 100 материалов, связанных с указанным активным местом, с опциональным фильтром по платформе.
 * @summary List place materials
 */
export type listPlaceMaterialsResponse200 = {
  data: MaterialListResponse;
  status: 200;
};

export type listPlaceMaterialsResponse400 = {
  data: ValidationErrorResponse;
  status: 400;
};

export type listPlaceMaterialsResponse404 = {
  data: PlaceNotFoundResponse;
  status: 404;
};

export type listPlaceMaterialsResponseSuccess = listPlaceMaterialsResponse200 & {
  headers: Headers;
};
export type listPlaceMaterialsResponseError = (
  | listPlaceMaterialsResponse400
  | listPlaceMaterialsResponse404
) & {
  headers: Headers;
};

export const getListPlaceMaterialsUrl = (
  { placeId }: ListPlaceMaterialsPathParameters,
  params?: ListPlaceMaterialsParams,
) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `${process.env.API_BASE_URL}/places/${placeId}/materials?${stringifiedParams}`
    : `${process.env.API_BASE_URL}/places/${placeId}/materials`;
};

export const listPlaceMaterials = async (
  { placeId }: ListPlaceMaterialsPathParameters,
  params?: ListPlaceMaterialsParams,
  options?: RequestInit,
): Promise<listPlaceMaterialsResponseSuccess> => {
  const res = await fetch(getListPlaceMaterialsUrl({ placeId }, params), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & {
      info?: listPlaceMaterialsResponseError['data'];
      status?: number;
    } = new globalThis.Error();
    const data: listPlaceMaterialsResponseError['data'] = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  const data: listPlaceMaterialsResponseSuccess['data'] = body ? JSON.parse(body) : {};
  return { data, status: res.status, headers: res.headers } as listPlaceMaterialsResponseSuccess;
};

/**
 * Выполняет временный redirect на сохраненный URL публичного материала. Endpoint не принимает внешний URL от клиента и работает только для материалов активных мест с безопасной https-ссылкой платформы.
 * @summary Redirect to material URL
 */
export type redirectMaterialResponse302 = {
  data: void;
  status: 302;
};

export type redirectMaterialResponse404 = {
  data: MaterialNotFoundResponse;
  status: 404;
};
export type redirectMaterialResponseError = (
  | redirectMaterialResponse302
  | redirectMaterialResponse404
) & {
  headers: Headers;
};

export type redirectMaterialResponse = redirectMaterialResponseError;

export const getRedirectMaterialUrl = ({ materialId }: RedirectMaterialPathParameters) => {
  return `${process.env.API_BASE_URL}/materials/${materialId}/go`;
};

export const redirectMaterial = async (
  { materialId }: RedirectMaterialPathParameters,
  options?: RequestInit,
): Promise<redirectMaterialResponse> => {
  const res = await fetch(getRedirectMaterialUrl({ materialId }), {
    ...options,
    method: 'GET',
  });

  const body = [204, 205, 304].includes(res.status) ? null : await res.text();
  if (!res.ok) {
    const err: globalThis.Error & {
      info?: redirectMaterialResponseError['data'];
      status?: number;
    } = new globalThis.Error();
    const data: redirectMaterialResponseError['data'] = body ? JSON.parse(body) : {};
    err.info = data;
    err.status = res.status;
    throw err;
  }
  const data: redirectMaterialResponse['data'] = body ? JSON.parse(body) : {};
  return { data, status: res.status, headers: res.headers } as redirectMaterialResponse;
};
